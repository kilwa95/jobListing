{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training.id }}</td>
            </tr>
            <tr>
                <th>Diplome</th>
                <td>{{ training.diplome }}</td>
            </tr>
            <tr>
                <th>Ecole</th>
                <td>{{ training.ecole }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ training.domaine }}</td>
            </tr>
            <tr>
                <th>PeriodeAt</th>
                <td>{{ training.periodeAt ? training.periodeAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('training_index') }}">back to list</a>

    <a href="{{ path('training_edit', {'id': training.id}) }}">edit</a>

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
